name: ci / lint

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Harness lint placeholder
        run: |
          echo "✅ [Harness Engineering] lint check passed (placeholder for now)"
          # TODO: Later replace with real lint (biome, eslint, ruff, shellcheck, etc.)